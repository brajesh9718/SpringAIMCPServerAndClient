apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-consumer-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-consumer-service
  template:
    metadata:
      labels:
        app: order-consumer-service
    spec:
      containers:
        - name: order-consumer-service
          image: youracr.azurecr.io/order-consumer-service:latest
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: app-config
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8082
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
